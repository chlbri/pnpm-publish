export const DEFAULT_FILTER = './';
export const SUMMARY_PATH = './pnpm-publish-summary.json';

export const NPM_WARNINGS = {
  '403': [
    'You cannot publish over the previously published versions',
    '403 Forbidden',
  ],
};
